[→ 6.4 · What the platform changes](6.4-platform-changes.md)<br>
[← 6.2 · Deploy A: the flag](6.2-deploy-flag.md)<br>
[↑ Part 6 · Agent Runtime](index.md)<br>
[Tutorial index](../../TUTORIAL.md)

---

# 6.3 · Deploy B: the `.env` route

*Part 6 · Agent Runtime*

Same script with `ENABLE_VIA_ENV=1`. It writes
`GOOGLE_CLOUD_AGENT_ENGINE_ENABLE_TELEMETRY=true` **and both content knobs**
into the temporary `.env`, and deploys with **no** `--otel_to_cloud`.

**Command:**

```bash
source env.sh
export ENGINE_ID_ENV=$(ENABLE_VIA_ENV=1 ./deploy/deploy_agent_engine.sh | sed -n 's/^ENGINE_ID=//p')
```

**Read this deployment's env list** with the same SDK command as 6.2, passing
`$ENGINE_ID_ENV`.

**Expected output** — exactly the lines `.env` carried, and nothing the CLI
added:

```console
GOOGLE_GENAI_USE_VERTEXAI=TRUE
GOOGLE_CLOUD_LOCATION=global
LOG_LEVEL=info
GOOGLE_CLOUD_AGENT_ENGINE_ENABLE_TELEMETRY=true
ADK_CAPTURE_MESSAGE_CONTENT_IN_SPANS=false
OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=NO_CONTENT
```

> [!IMPORTANT]
> **What it means.** In 6.2 the CLI added `ADK_CAPTURE_MESSAGE_CONTENT_IN_SPANS=false`
> on its own. Here all three telemetry lines came from `.env`, including the
> event knob the flag route never set. On the `.env` route the CLI adds no
> content knobs, so every knob is yours. Had the script not written the span
> knob, prompt text would ride on the spans.

---

[→ 6.4 · What the platform changes](6.4-platform-changes.md)<br>
[← 6.2 · Deploy A: the flag](6.2-deploy-flag.md)<br>
[↑ Part 6 · Agent Runtime](index.md)<br>
[Tutorial index](../../TUTORIAL.md)
