[→ 5.5 · Your own server: install the exporters yourself](5.5-your-own-server.md)<br>
[← 5.3 · The same flag on `adk api_server`](5.3-api-server.md)<br>
[↑ Part 5 · OpenTelemetry](index.md)<br>
[Tutorial index](../../TUTORIAL.md)

---

# 5.4 · The same flag on Cloud Run: `adk deploy cloud_run --otel_to_cloud`

*Part 5 · OpenTelemetry*

> [!NOTE]
> **Why you are here.** 5.2 and 5.3 ran the flag on your laptop under your ADC
> login. This section ships the same flag to Cloud Run and reads the same
> `gen_ai.*` events back, produced by a process running under a service account.

`adk deploy cloud_run --otel_to_cloud` bakes the flag into the container's start
command (`cli/cli_deploy.py:216`), so the server inside Cloud Run installs the
same exporters it installed on your laptop. No agent code, no `OTEL_*`
variables in the deploy.

**Step 1 — Deploy.**

**Command:**

```bash
export PROJECT_ID="$(gcloud config get-value project)"
export REGION=us-central1

.venv/bin/adk deploy cloud_run \
  --project="$PROJECT_ID" \
  --region="$REGION" \
  --service_name=adk-logging-otel \
  --otel_to_cloud \
  ./demo_agent \
  -- \
  --allow-unauthenticated \
  --set-env-vars="GOOGLE_GENAI_USE_VERTEXAI=TRUE,GOOGLE_CLOUD_PROJECT=$PROJECT_ID,GOOGLE_CLOUD_LOCATION=global"
```

Everything after `--` passes straight through to `gcloud run deploy`. Watch the
build finish and note the service URL it prints. `GOOGLE_CLOUD_LOCATION=global`
appears here even though `.env` already has it. See
[Where .env goes and why the location is repeated](#where-env-goes-and-why-the-location-is-repeated).

**Step 2 — Send one turn.** Grab the service URL, then curl it.

**Command:**

```bash
export URL=$(gcloud run services describe adk-logging-otel \
  --project="$PROJECT_ID" --region="$REGION" --format='value(status.url)')

curl -s -X POST "$URL/apps/demo_agent/users/u1/sessions/s5-run" \
     -H 'content-type: application/json' -d '{}'

curl -s -X POST "$URL/run" \
     -H 'content-type: application/json' \
     -d '{"app_name":"demo_agent","user_id":"u1","session_id":"s5-run",
          "new_message":{"role":"user","parts":[{"text":"What'\''s the weather in London?"}]}}'
```

If the curl errors, read the build output rather than trusting the deploy. See
[Two deploy traps](#two-deploy-traps).

**Step 3 — Read the events back.** Open **Logs Explorer**, turn on **Show
query**, and run the same query as 5.2 in the **Query editor**:

**Command:**

```
logName=~"gen_ai\."
```

The same events appear, one `gen_ai.client.inference.operation.details` per
`call_llm`, now emitted from Cloud Run. Content is off (`<elided>`) because
`demo_agent/.env`'s `ADK_CAPTURE_MESSAGE_CONTENT_IN_SPANS=false` shipped inside
the image. On Cloud Run the exporter labels these entries with a `generic_task`
resource whose job is the service name, so the log name is what you filter on.

**Step 4 — Tear down.**

**Command:**

```bash
gcloud run services delete adk-logging-otel --project="$PROJECT_ID" --region="$REGION" --quiet
```

5.5 is the one place in this part where the CLI is **not** starting the process,
so you install the exporters yourself.

## Deep dives

### Where .env goes and why the location is repeated

`adk deploy cloud_run` copies the whole agent folder, including
`demo_agent/.env`, into the container image, so your local settings ship
automatically. But `.env` is a file ADK loads, and a real environment variable
beats it. ADK's generated Dockerfile sets one: `ENV GOOGLE_CLOUD_LOCATION` to the
*service's* region, `us-central1` (`cli/cli_deploy.py:193`). That is wrong for
the **model**, which runs in `global`, and it overrides the `global` in your
`.env`. `GOOGLE_CLOUD_LOCATION=global` in `--set-env-vars` is also a real env
var, applied last, so it wins.

### Two deploy traps

- `adk deploy` exits `0` even when the deploy failed
  (`cli/cli_tools_click.py:2456`). The curl in Step 2 is the real check.
- The container needs `google-adk[otel-gcp]` or it boot-crashes on the
  unconditional Cloud Logging import (`telemetry/google_cloud.py:272`).
  `demo_agent/requirements.txt` already pins it.

---

[→ 5.5 · Your own server: install the exporters yourself](5.5-your-own-server.md)<br>
[← 5.3 · The same flag on `adk api_server`](5.3-api-server.md)<br>
[↑ Part 5 · OpenTelemetry](index.md)<br>
[Tutorial index](../../TUTORIAL.md)
