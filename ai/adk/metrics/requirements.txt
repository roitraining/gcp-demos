google-adk[otel-gcp]>=2.8.0
opentelemetry-exporter-otlp-proto-http
# adk web --otel_to_cloud sets up all three signals (trace, metrics, logs); the
# otel-gcp extra pulls only the instrumentation, not these Cloud exporters.
opentelemetry-exporter-gcp-logging
opentelemetry-exporter-gcp-monitoring
opentelemetry-exporter-gcp-trace
# adk deploy agent_engine (2.5) packages and pushes from your machine, so the
# Agent Engine SDK must be installed locally.
google-cloud-aiplatform[agent-engines]
google-cloud-bigquery
google-cloud-bigquery-storage
google-cloud-storage
bigquery-agent-analytics
# examples/plot_tokens.py (2.1) renders the token series as a PNG so a reader can
# set the API's chart beside the Console's.
matplotlib
