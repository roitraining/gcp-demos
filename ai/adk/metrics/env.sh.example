# Copy to env.sh, set your project, then `source env.sh` in each shell you use.
# These are the shell variables the tutorial's gcloud and curl commands read.
# The running agent reads its own model config from .env / demo_agent/.env.

export PROJECT_ID=your_project
export GOOGLE_CLOUD_PROJECT=$PROJECT_ID   # the ADK/genai runtime reads this name
export REGION=us-central1
export LOCATION=$REGION

# The metrics pipeline needs a valid prometheus_target resource locally.
# adk web --otel_to_cloud reads these; a bare script also needs gcp.project_id
# (page 2.4). See tutorial/00-setup.md.
export OTEL_SERVICE_NAME=adk-metrics
export OTEL_RESOURCE_ATTRIBUTES="service.instance.id=laptop-1,cloud.region=${REGION}"
